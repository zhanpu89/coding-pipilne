# 索引规则

## 设计原则

- 等值条件列先，范围条件列后，高选择性优先
- 覆盖索引（查询字段均在索引中）
- 最左前缀匹配，手机号/邮箱做前缀索引

## 禁止

- 低选择性(<5%) 独立索引
- 复合索引 >5 字段
- 重复索引（已有 (a,b) 再加 (a)）
- TEXT 全列索引（用前缀索引替代）

## 必建索引

唯一约束 / 外键关联 / 租户 tenant_id / 状态枚举 / 时间范围查询 / 排序字段 / 软删除 (`deleted_at` 不单独索引，与查询字段建复合)
