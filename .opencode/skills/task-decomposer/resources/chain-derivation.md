# 功能链完整性推导规则

Step 2.5 加载。7 条规则逐模块检查接口清单，发现缺口则补充。

| 规则 | 内容 | 不通过 |
|------|------|--------|
| 规则零 | 需求缺口：PRD 业务步骤/功能→接口正向比对 | 补充缺失接口 |
| 规则一 | PUT/DELETE/PATCH 是否有对应 GET（回显/确认） | 补充接口 |
| 规则二 | 状态机：枚举值完整 + 校验规则 + 逆路径（通过→驳回→重提） | 补充 |
| 规则三 | 跨模块依赖：精确到接口路径 | 逐条列出 |
| 规则四 | 数据生命周期：创建→查询→修改→删除→恢复 | 补充解绑/恢复/查询 |
| 规则五 | 异步流程：异步任务状态查询 + 死信处理 + 超时回调 | 补充 |
| 规则六 | 权限与数据隔离：多租户/归属审核 → 标注到 Step 3 验证 | 标注 |

输出推导报告 → 用户确认 → 锁定接口清单。确认前禁止 Step 3。
